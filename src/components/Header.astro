<header id="siteHeader"><h1>header</h1></header>

<style lang="scss">
	#siteHeader {
		position: fixed;
		z-index: var(--layer--above);
		top: 0;
		width: 100%;

		background-color: rgb(0 0 0 / 60%);
		backdrop-filter: blur(18px) saturate(150%);
		// box-shadow: 0 0 40px rgb(0 0 0 / 5%);

		// will-change: transform;
		// transition: transform 500ms var(--easeOut), opacity 500ms var(--easeOut);
	}
</style>
